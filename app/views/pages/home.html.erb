    <%= render "banner" %>
<br>
<br>
<center><h1>Why use Batch-It?</h1></center>
<div class="front-info">
  <div class="info-box">
            <div class="info-left">
<i class="fa fa-rocket fa-5x" aria-hidden="true" ></i></div>
        <div class="info-right">
Batch-It helps you plan a bachelor trip in no time and takes all the hassle of coordinating with friends and of choosing places and activities out of the equation.</div>
  </div>

  <br>

  <div class="info-box">
    <div class="info-left">
<i class="fa fa-star fa-5x" aria-hidden="true"></i></div>
    <div class="info-right">
      Batch-It has already pre-curated a list of the best restaurants, bars, activities and places to stay
    </div>
  </div>

<br>

  <div class="info-box">
        <div class="info-left">
<i class="fa fa-users fa-5x" aria-hidden="true"></i></div>
        <div class="info-right">The best man sets the basics, but everyone on the trip can get involved.</div>
  </div>

  <center><h1>How it works</h1></center>

<div class="info-box">
  <div class="info-left2">
    <div><p>The trip-planner can easily set up an account and pre-select the choices for the group.
    He choses two cities, and for every city three restaurants, bars, accommodations and activities out of which the other trip participants can chose their favourite two. In the end you will be able to see the voting results and can easily book the trip.</p></div>
  </div>

  <div class="info-right">
    </div>
    <%= image_tag "screenshot-batchit.png", style: "height: 270px; width:550px; margin-right: 100px; margin-top: 75px;"%>
    </div>
  </div>

<div class="footer"><strong>About Team Contact</strong></div>

  <% content_for(:after_js) do %>
    <script>
      $("#create").click(function(){
          $(".banner-content").addClass("hidden");
          $("#hidden-form").removeClass("hidden");
          $('html, body').animate({
            scrollTop: $("#hidden-form").offset().top
          }, 1000);
      });

      /* create datepicker for start date and end date */
      <% min_date = Date.today.strftime('%-m-%-d-%Y')  %>

      $('#datepicker_end_date').datepicker({
        dateFormat: 'dd/mm/yy',
        constrainInput: true
      });

      $('#datepicker_start_date').datepicker({
        dateFormat: 'dd/mm/yy',
        constrainInput: true,
        minDate: new Date(<%= raw [min_date] %>),
        onClose: function(selectedDate) {
          $("#datepicker_end_date").datepicker("option", "minDate", selectedDate );
          $("#datepicker_end_date").focus();
        }
      });
    </script>
  <% end %>


