<% if @thing.errors.any? %>
  // Render new review form with errors
  $('#new_thing').html('<%= j render "cities/new_accommodation_form" %>')
<% else %>
  // Add it to the DOM, at the end of the existing reviews. It's still hidden.
  $("#accommodations-selection").html('');
  <% i = 1 %>
  var new_cards = ""
  <% @things.each do |thing| %>
    new_cards = new_cards + '<%= j render "cities/updated_thing_selection", thing: thing, i: 1, type: @thing.thing_type, empty: false %>'
    <% i = i + 1  %>
  <% end %>
  <% while i <= 3 do %>
    new_cards = new_cards + '<%= j render "cities/updated_thing_selection", type: @thing.thing_type, i: i, empty: true %>'
    <% i = i + 1 %>
  <% end %>
    new_cards = $(new_cards).hide();
  $("#accommodations-selection").append(new_cards);
  new_cards.fadeIn();
  accommodations_qty = '<%= @things.size %>' + ' '
  $("#accommodation-status-number").html(accommodations_qty)
  if (accommodations_qty == '3 '){
    $("#accommodation-tab>.circle-status").addClass("full");
  }
<% end %>
